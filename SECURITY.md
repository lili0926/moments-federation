# 安全须知

这是一份**诚实的**清单：说清楚现在挡得住什么、挡不住什么，以及部署前必须做的几件事。
已知没做的事也写在这里 —— 写下来才算数，不写就只是忘了。

---

## ⚠️ 已知隐患：默认没有 TLS

**这是目前唯一一条「拦着它称为成品」的问题。**

联邦请求有 HMAC 签名，所以**完整性和来源**是可靠的：别人改不了内容，也伪造不了身份。
但签名**不保证机密性**。跑在明文 HTTP 上时：

- 你和好友的动态正文、评论，在网络链路上是明文的
- **`ADMIN_TOKEN` 每个请求都明文发一遍**。拿到它 = 拿到这个节点的完整控制权
  （发动态、加好友、删东西、看全部时间线）

在公共 WiFi 上用手机连自己的节点，同一个网络里的人抓包就能拿到那把 token。

**修法是一张证书，不是改代码。** `backend/nginx.conf.example` 里 443 的骨架已经写好了，
有域名的话 Let's Encrypt 十分钟就能签下来：

```bash
sudo certbot --nginx -d moments.example.com
```

签完把 `.env` 里的 `SELF_SERVER_URL` 改成 `https://...`，重启节点。
**注意好友表里存的是握手当时的地址** —— 换协议之后老好友要重新握手，或者手动改库。

> 纯 IP 的节点签不了证书，得先有域名。
> 在那之前：别在不可信的网络里用，别把 `ADMIN_TOKEN` 当长期安全的东西。

---

## 三层边界

搞清楚这三层，才能判断某个口该不该放进 nginx。

| 层 | 路径 | 谁能调 | 靠什么挡 |
|---|---|---|---|
| 联邦 | `/api/moments/receive`、`/sync`、`/action`、`/api/friends/request`、`/accept-callback` | 公网任何人 | HMAC 签名 + 时间窗（握手那两个口是无凭据的，见下） |
| 管理 | `/api/admin/*` | 本人（手机 / 浏览器） | `ADMIN_TOKEN` |
| 内部 | `/internal/*`、`/api/moments/publish`、`/feed` | 只有本机 | `localhostOnly` **+ nginx 白名单里没有它们** |

**`localhostOnly` 不是一道真正的防线。** `app.set('trust proxy', false)` 意味着经 nginx
反代进来的请求 `req.ip` 就是 `127.0.0.1`，一样能过。**真正挡住 `/internal/*` 的是
nginx 白名单里没有它。** 所以：**永远不要把 `/internal/` 加进 nginx 配置。**

---

## 部署前的 checklist

- [ ] **`ADMIN_TOKEN` 用随机值**：`openssl rand -hex 32`。别用能猜的词
- [ ] **上 TLS**（见上）
- [ ] **nginx 用白名单式配置**，最后一条是 `location / { return 404; }`。
      照抄 `backend/nginx.conf.example`，别自己从头写
- [ ] **别在 nginx 里给 `/api/admin/` 加 `Access-Control-Allow-Origin`**。
      Node 侧已经加了一份，浏览器见到两个 ACAO 直接判 CORS 失败 ——
      而请求其实早就在服务端执行完了，现象是「日志全 200，前端却说连不上」，极难查
- [ ] **删掉 `ALLOW_PRIVATE_PEERS`**（如果你为了本机自测加过）。它放开的是 SSRF 闸
- [ ] **`PHASE=2`** 才算逻辑上启用联邦
- [ ] **确认 Node 直接监听的端口没暴露公网**。`app.js` 里写死 `listen(PORT, '0.0.0.0')`，
      也就是说它在所有网卡上都听着 —— 挡住它的是防火墙/安全组，不是代码。
      只放行 nginx 那个端口
- [ ] **定期备份** `backend/data/moments.db`（单个 SQLite 文件，`scripts/backup.sh` 有现成的）

---

## 挡得住什么

下面每条都有对应的测试（`npm test`），改坏了会当场红。

**陌生人（不带任何凭据）：**

- 拿邀请码只能**发起申请**，进待审列表，必须本人点同意才成为好友
- 不能用一个已有好友的 `node_id` 把那段关系改掉或打回 pending
  （`friend_id_taken_by_another_server`）
- 不能先塞一个自己生成的 token、再拿它调 `/accept-callback` 自批准
  （那个口只认本机主动发起的 `direction='out'`）
- 不能让节点去 fetch 内网地址（`isPrivateHost`，默认拦回环 / 私网 / 链路本地 / 云元数据段）
- 申请按来源 IP 限流，另有一道全站上限
  （**key 用的是 nginx 设的 `X-Real-IP`，不是 body 里自报的字段** —— 后者换一个就是新桶）

**已经加上的好友（有 `shared_secret`）：**

- 推来的动态，作者一律记成**签名证明的那个节点**，body 里自报的 `author_id` 不作数
- **显示名也不收自报的**，取握手时存下的身份表（本人设的备注优先）。
  只修 `author_id` 是不够的：人眼看到的名字才是判断依据
- 身份（`author_identity_id`）必须确实挂在那个节点名下，否则记 `unknown`
- 只能删自己推来的动态
- 单条正文 10000 字、评论 1000 字、每个好友最多缓存 500 条（超了删他最旧的）

**邀请码和 token 是两回事，别混：**

- **邀请码**（`MF1:...`）是名片：base64 编码的 `{node_id, server_url, display_name}`，
  内容不变所以每次都一样。**不是密码**，base64 也不是加密，谁拿到都能解开。
  它可以随便发微信 —— 因为它什么权限都没有
- **`verify_token`** 是一次性回执单号：随机 32 字节、10 分钟过期、用一次作废，
  作用是把「这一次申请」和「对方稍后的回调」绑在一起。**它必须不固定，因为它是凭据**
- **`shared_secret`** 才是长期钥匙，本人点同意那一刻才生成，之后所有联邦请求靠它签名

---

## 挡不住什么（威胁模型的边界）

**这套东西的前提是「熟人互信」。** 加好友是人工审批的，一旦加了，就等于接受了：

- 好友能看到你**所有**的公共动态，**包括加好友之前发的**
  （`/api/moments/sync` 是按时间拉的，对方传 `since=0` 就拿全部历史）。
  想改成「只给加好友之后的」，把 `.env` 的 `SYNC_ONLY_AFTER_FRIENDSHIP` 设成 `1`
- 好友节点被攻破 = 那个好友能做的事攻击者都能做（在上面那些限制之内）
- 内容没有端到端加密。服务端能读到全部内容，节点主人也能

**不在防护范围内的：**

- 拿到 `ADMIN_TOKEN` 的人 = 节点主人。没有二次确认，没有操作审计
- 服务器本身被入侵（数据库是明文 SQLite，`shared_secret` 就在里面）
- 限流计数在进程内存里，**重启归零**。挡的是脚本连打，不是有耐心的人

---

## 报告问题

这是个私人项目，没有安全响应流程。发现问题就开 issue，或者直接改。
