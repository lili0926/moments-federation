# 当前阶段：1 / 2 / 3
PHASE=1

PORT=3000

# 本节点对外暴露的地址（好友推送时对方要能连到这个地址）
SELF_SERVER_URL=https://moments.example.com
SELF_NODE_ID=alice_node
SELF_DISPLAY_NAME=Alice

# 数据库文件路径
DB_PATH=./data/moments.db

# 防重放：签名时间窗口（秒）
SIGNATURE_WINDOW_SECONDS=300

# 加好友请求限流：同一来源每小时最多几次
FRIEND_REQUEST_RATE_LIMIT=5

# 防无限刷评论：同一动态里，两人之间最多往返几轮
MAX_EXCHANGE_ROUNDS=2
